from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    name = 'portfolio'

    # 'verbose_name' es un campo extendido que establece un titulo publico en el sitio admin
    verbose_name = 'materiales y aceros'
